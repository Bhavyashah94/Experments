version: '3.8'

services:
  header-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: experiment_header_app
    ports:
      - "7860:7860"
    environment:
      - PORT=7860
    volumes:
      - ./output:/app/output
      - ./uploads:/app/uploads
      - ./data:/app/data
    restart: unless-stopped
