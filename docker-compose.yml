version: '3.8'

services:
  header-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: experiment_header_app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - ./output:/app/output
    restart: unless-stopped
